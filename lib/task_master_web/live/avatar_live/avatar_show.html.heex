<.header>
  Avatar <%= @avatar.id %>
  <:subtitle><%= gettext("This is a avatar record from your database.") %></:subtitle>
  <:actions>
    <.link
      patch={~p"/#{@current_user.id}/avatars/#{@avatar}/show/edit"}
      phx-click={JS.push_focus()}
    >
      <.button><%= gettext("Edit Avatar") %></.button>
    </.link>
  </:actions>
</.header>

<.list>
  <:item title="Path"><%= @avatar.path %></:item>
  <:item title="User"><%= @avatar.user_id %></:item>
  <:item title="Is active"><%= @avatar.is_active %></:item>
</.list>

<.back navigate={~p"/#{@current_user.id}/avatars"}><%= gettext("Back") %></.back>

<.modal
  :if={@live_action == :edit}
  id="avatar-modal"
  show
  on_cancel={JS.patch(~p"/#{@current_user.id}/avatars/#{@avatar}")}
>
  <.live_component
    module={TaskMasterWeb.AvatarLive.AvatarComponent}
    id={@avatar.id}
    title={@page_title}
    action={@live_action}
    avatar={@avatar}
    patch={~p"/#{@current_user.id}/avatars/#{@avatar}"}
  />
</.modal>
